# M4a T18 联调冒烟记录（2026-09-09）

> 来源：docs/plans/M4a-marketplace.md 板块 F T18。断言全链真实数据（seed 直插——非 mock）。
> 契约真相源：design §5.2；测试策略：typecheck + 冒烟（plan §4 决策 3，无单测框架）。

## 1. 联调数据 seed（一次性脚本，/private/tmp 已用后清理）

dev 库原两只冒烟资产均为零版本空壳，无法覆盖版本/文件内容/compare 链。按 server 内部形状直插
（复用 schema/db + storageKey 规则 `{nsId}/{assetId}/{vid}/{path}`——versions.ts:111 实证；
bundle 副本 `{vid}/bundle.zip`）：

| 资产 | type | 版本 | 文件 | 用途 |
|------|------|------|------|------|
| smoke-ns/demo-rag-skill (id 130) | skill | v1.0.0 (id 76) + v1.1.0 (id 77) PUBLISHED | SKILL.md（两版内容差异）/lib/embedding.ts /reference/guide.md（仅 v1.0）/scripts/search.mjs（仅 v1.1） | 版本历史、文件树、行级对比三型（MODIFIED/ADDED/DELETED） |
| smoke-ns/demo-http-mcp (id 131) | mcp | v1.0.0 (id 78) PUBLISHED | README.md /src/server.mjs /lib/utils.mjs | 总览 README 路径、单版本版本 tab 空态 |

注：latestName/latestDescription 投影源为 `parsedMetadataJson`（service.ts:88-100 实证），
seed 初版写错 manifestJson 后修正——断言驱动修复。

## 2. 契约全链断言（t18-chain.ts，匿名面）29/29 PASS

列表（含新资产 + type 过滤）· 详情（PUBLIC/ACTIVE/latestVersion 1.1.0/latestName 投影/
downloadCount 自增≥1284）· 版本列表（total 2 降序/行形状）· 版本详情（files 3 + sha256 64 位）·
文件内容 G7（SKILL.md 含 v1.1 特性 + md 表格）· compare（三型齐/ADDED=search.mjs/DELETED=
guide.md/+− 行真内容/行号对形状）· compare from=to → 200 空 files（T6 漂移真相复验）·
download（zip 头 + bundle 非空）· mcp README 链 · stats（totalAssets 4 / skill 3 / mcp 1）·
labels 200。

## 3. 浏览器 dogfood（Edge headless CDP——本机无 Chrome，browser-use 授权墙改原生 CDP 驱动）
26/26 PASS + NO CONSOLE ERRORS，截图见本目录 1-home … 6-mcp-detail：

- 首页：hero/统计 资产总数 4/最新发布含 demo/类型入口 href 三中心
- /skills：placeholder/真实数据卡/共 3 个技能/排序栏
- 详情（skill）：latestName/@ns/总览 md 渲染（表格+特性）/下载 v1.1.0/元信息
- 文件 tab：目录行/预览对话框（SKILL.md 正文 + role=dialog）→ 关闭
- 版本 tab：历史两行/diff 三型徽章/searchByPrefix +− 行
- mcp 详情：README 渲染/单版本版本 tab 暂无数据

## 4. 修复（随本 Task commit）

- **useApi StrictMode abort 竞态（真 bug）**：React 19 dev StrictMode 双跑 effect 时 cleanup
  即 abort 会让双跑第二波 fetch 全部以 AbortError 落败（页内 fetch 包装实证：
  `AbortError: signal is aborted` 且无第二次请求发出；StrictMode 摘除对照实验通过）。
  修复：abort 属锦上添花——竞态终止本就由 alive 守卫承担，cleanup 不再 abort，
  signal 改惰性（new AbortController().signal 永不手动 abort）。断言驱动 + 双态验证
  （StrictMode 保留）。

## 5. 走查限制注记

浏览器 = Edge headless（用户无 Chrome）；渲染截图已入档本目录。交互点击（tab 切换/对话框/
diff 渲染）由 CDP Runtime.evaluate 驱动真实 DOM。控制台零错误。
