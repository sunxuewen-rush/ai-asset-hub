# M4b-4 个人面 B 批（我的资产 / 工作台 landing）· 验收硬证据

> **本文件 = 本批出口件的证据记录**（批 plan T11 断言①-⑦ · 批 design §9.3/§9.4/§9.5）。
> 所有数字一律为**实测产出**（命令 / 脚本 / 真浏览器），非人工点数；行数一律 `wc -l`。
>
> **状态：⬜ 骨架（2026-09-18 立项时建立）—— 执行期逐节回填**。本文件由批 plan T11 的步骤 4 产出；
> 在 T1-T10 落地前，各节保持「待回填」，**不得**预填任何未经实测的数字。
>
> ⚠️ **口令纪律**：脚本口令一律从 env 读（`SMOKE_*_PASSWORD` 惯例），**仓库内零口令 / 零连接串**（本文件亦不含值）。

## 1. 五门禁（CI 顺序复现 · 断言②）

| 步骤 | 命令 | 结果 |
|------|------|------|
| 依赖 | `bun install --frozen-lockfile` | 待回填 |
| 类型 | `bun run typecheck` | 待回填 |
| 静态 | `bun run lint` | 待回填 |
| 格式 | `bun run format:check` | 待回填 |
| 文档体检 | `bun docs/smoke/scripts/doc-audit.ts` | 待回填 |
| 构建 | `bun run build` | 待回填 |
| 迁移 | `bun run db:migrate` | 待回填（本批**零迁移**，仍跑守门） |
| 测试 | `CI=true bun run test` | 待回填（用例数 ≥ 基线 + 新增；**0 fail**） |

## 2. 门户零回归 + 链冒烟（断言③）

| 项 | 结果 |
|----|------|
| `m4a-dogfood.ts` | 待回填（目标 **36/36 + NO JS ERRORS**） |
| `m4a-chain-smoke.ts` | 待回填（目标 **PASS**） |
| `CenterPage.tsx` diff | 待回填（目标 **为空** —— 共享 hook 扩展须零改动门户调用点） |

## 3. 本批 dogfood：G1-G15（断言① · 新建 `docs/smoke/scripts/m4b4-personal-b-dogfood.ts`）

| 组 | 断言 | 结果 |
|----|------|------|
| G1 | 未登录访两页 ⇒ 重定向 `/login?next=<带码路径>` | 待回填 |
| G2 | `role = 1` 工作台：1 卡 + 1 请求 | 待回填 |
| G3 | `role = 10` 工作台：3 卡 + 3 请求；审计卡 5 行无操作人 | 待回填 |
| G4 | 我的资产默认「全部」⇒ 实际请求含 `status=ALL` | 待回填 |
| G5 | **owner-only 集合**：他人资产不出现 | 待回填 |
| G6 | **九列**齐（含标签/下载/收藏）+ 状态列三态正确 | 待回填 |
| G7 | 抽屉打开即 2 请求；状态治理 3×2 按钮集 | 待回填 |
| G8 | 危险区禁用真值（含 `PUBLISHED` ⇒ 禁用；加载中保持禁用） | 待回填 |
| G9 | 版本段可删 4 态 / 禁删 3 态；yank 仅 `PUBLISHED` 行；`role < 10` 禁用 | 待回填 |
| G10 | 标签候选不含 PRIVILEGED；chips ≥ 10 禁用 | 待回填 |
| G11 | 筛选/搜索 ⇒ `page` 回落 1 + URL 同步 | 待回填 |
| G12 | 门户零回归 36/36 + chain-smoke PASS | 待回填 |
| G13 | 全场景 `NO JS ERRORS` | 待回填 |

## 4. 造数（可重放 · 断言①/④ 的前提）

> 脚本 `docs/smoke/scripts/m4b4-seed-assets.ts`（幂等 upsert）· **写库前须用户授权**（批 plan §5）。

```
（待回填：造数命令 + 产出清单 —— 3 角色 / owner 三态资产 / 他人 ACTIVE 资产 /
  含 PUBLISHED 与仅 DRAFT 的版本 / 标签挂载）
```

## 5. 整体审计（出口件 ⑤ · 全仓覆盖式扫描）

> 维度：死导出 · i18n 键 · 已删件残留 · 类串重复 · 越轴值 · token 消费者 · 注释腐化 ·
> 文档数字实测 · 官方件硬规则 · 既有登记项状态 · 旧口径/术语指针。

| 项 | 结果 |
|----|------|
| 扫描命令 | 待回填 |
| findings 清单 | 待回填（逐条登记 + 处置：修 / 订正 / 回填 / 口径登记） |
| 未决项 | 待回填（目标 **0**） |

## 6. 审计发现（如有 · 诚实清单）

（待回填）

## 7. 权威行数表（`wc -l` 实测 · **本表 = 本批文件行数的唯一权威源**）

| 文件 | 行数 |
|------|------|
| 待回填 | 待回填 |

## 8. 出口五件状态

| # | 件 | 状态 |
|---|----|------|
| ① | 批 design 8 维 ≥9（定稿 **9.66**） | ✅ 定稿轮已达成（执行期 converge 重评待回填） |
| ② | **T1–T14** Task 全绿 | 待回填 |
| ③ | 五门禁逐项 exit 0 | 待回填 |
| ④ | dogfood / 观感（合规核对，审美归 M4b-7） | 待回填 |
| ⑤ | 整体审计（无未决项） | 待回填 |

## 9. 文档-代码对齐重评（converge）

（待回填：设计层 ↔ 代码回查 + 8 维重评）

## 10. 未证项 / 登记项（诚实清单）

| # | 项 | 归属 / 说明 |
|---|----|------------|
| 1 | 抽屉**无「提交审核」入口**（提审端点已在，上传无 UI ⇒ 链路不闭合） | **M4b-8** 发布批（R2 已翻转入册） |
| 2 | **PRIVILEGED 标签的 × 不做禁用特判**（详情 `labels[]` 只返 slug ⇒ 无法判定 type） | 服务端 `label.access_denied` 兜底；精确禁用归 **M4b-6** |

## 11. 人工验收（**待用户实机**）

（待回填：用户实机结论 + 日期；**审美面不在本批范围** ⇒ 只做合规核对：有无错位 / 溢出 / 串色 / 异常）

## 12. 原型评审（DEV-only · **物料不进仓**）

> 原型落点：`apps/web/src/pages/__proto/M4b4Proto.tsx`（列表 + 抽屉视图 `/__proto/m4b4`）·
> `apps/web/src/pages/__proto/M4b4DetailProto.tsx`（详情页管理区视图 `/__proto/m4b4/detail`）
> —— **两个文件与 `main.tsx` 的 DEV 路由行均为评审物料，提交前删除**（`import.meta.env.DEV` 门控）。

| 项 | 值 |
|----|----|
| 评审方式 | 可点原型（真仓真件 + **假数据** · 零 API 请求）· 用户逐条指令微调 |
| 轮次 | **1 轮收口**（2026-09-18）· 用户结论：「**ui 部分差不多了**」 |
| 逐条决定 | **R1–R23** —— 明细与沿革见批 design **§2.1d**（含作废留痕：`⋯` 菜单 / 四段抽屉 / `标签挂载` 段名等） |
| 形态覆盖 | 列表 9 列 · 抽屉纯预览（描述 + 标签+- + 统计行 + 「完整详情 ↗」）· 详情页（头卡 [下载][收藏] + 右栏 元信息/标签+-/管理卡）· 管理区 **5 档权限** · 「隐藏 / 禁用+说明」两形态 |
| 原型侧实测（自测） | 抽屉宽 **560px** · 类型列 `bg-type-*` 计数 **0** · 操作列图标钮 32×32（`aria-label`/`title`）· 标签 chips `displayName` 渲染 · 管理区逐档按钮集（owner 无 yank / 管理档有 yank / 超管 + 特权标签）· 版本 Tab 删除态 owner 2 / 管理档 4 —— 均以真浏览器 CDP 断言取得 |
| 环境问题（登记） | 原型内 **toast 不渲染**（sonner 容器存在、`[data-sonner-toast]` = 0；列表页既有路径同样如此）⇒ 待查，**不影响形态评审** |
| 物料清理 | ⬜ **待执行**（提交前删 `__proto/` 两文件 + `main.tsx` DEV 路由行与 import） |

**留待明确口令的项（⬜ 不当已通过）**

| # | 项 | 现状 |
|---|----|------|
| 1 | 管理区**动作层**显隐取「不渲染」还是「禁用 + `title` 说明」 | 原型两形态均可看；批 design §4.6 记为「实现期取一」⬜ |
| 2 | `labels` 带 `type` 后，**PRIVILEGED 的 × 是否升级为精确禁用**（原 Q13 = A） | 技术上已可闭合（批 design §11.2 F1 补注）；**默认仍按 Q13 = A** ⬜ 待拍板 |
| 3 | 状态文案 `活跃` vs 对标 skillhub 的 `正常` · 版本 8 态 3 处措辞 | 登记 ⬜ 待定 |
| 4 | star 能力（列表列 / 抽屉统计 / 详情页按钮依赖） | ⬜ **待立批**（用户决定：UI 收尾提交后先做 star；建议 `M4-star`，执行序在 M4b-4 前） |
